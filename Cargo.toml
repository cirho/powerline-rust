[package]
name = "powerline"
version = "0.1.0"
authors = ["Xeoeen <Xeoeen@users.noreply.github.com>"]

[dependencies]
users = "0.7.0"
libc = "0.2"


[features]
default = ["bash-colors"]
bash-colors = []
bare-colors = []
